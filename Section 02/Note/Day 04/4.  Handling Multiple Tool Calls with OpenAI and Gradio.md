# 🔧 Handling Multiple Tool Calls with OpenAI and Gradio

অনেক সময় user এমন প্রশ্ন করে যেখানে **একাধিক tool call** লাগতে পারে।

উদাহরণ:

> “Dhaka থেকে Dubai-এর flight খুঁজে দেখাও এবং cheapest ticket টা book করো”

এখানে AI কে করতে হবে:

1. ✈️ flight search
2. 💰 cheapest flight identify
3. 🎟️ booking tool call

মানে **multi-step workflow**।

---

# 🧠 Single Tool vs Multiple Tool Calls

| Type          | Example                  |
| ------------- | ------------------------ |
| Single Tool   | শুধু flight status check |
| Multiple Tool | search → select → book   |

---

# ⚙️ Workflow Overview

```id="4cgn55"
User Request
    ↓
LLM decides tools
    ↓
Tool 1 executes
    ↓
Tool result returned
    ↓
LLM decides next tool
    ↓
Tool 2 executes
    ↓
Final response
```

---

# 🧰 1. Install Packages

```bash id="ef79b2"
pip install openai gradio
```

---

# 🛠️ 2. Create Airline Tools

```python id="ejg8f9"
def search_flights(source, destination):
    return [
        {"flight": "BG101", "price": 12000},
        {"flight": "BG202", "price": 10000},
        {"flight": "BG303", "price": 15000}
    ]


def book_ticket(name, flight_no):
    return {
        "message": f"Ticket booked for {name}",
        "flight": flight_no
    }
```

---

# 🧾 3. Define OpenAI Tool Schemas

```python id="xv7tb3"
tools = [
    {
        "type": "function",
        "function": {
            "name": "search_flights",
            "description": "Search available flights",
            "parameters": {
                "type": "object",
                "properties": {
                    "source": {"type": "string"},
                    "destination": {"type": "string"}
                },
                "required": ["source", "destination"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "book_ticket",
            "description": "Book a flight ticket",
            "parameters": {
                "type": "object",
                "properties": {
                    "name": {"type": "string"},
                    "flight_no": {"type": "string"}
                },
                "required": ["name", "flight_no"]
            }
        }
    }
]
```

---

# 🤖 4. Multi-tool Handling Logic

এটাই সবচেয়ে গুরুত্বপূর্ণ অংশ।

```python id="mhvcwi"
from openai import OpenAI
import json

client = OpenAI()

available_functions = {
    "search_flights": search_flights,
    "book_ticket": book_ticket
}

def run_agent(user_input):

    messages = [
        {"role": "user", "content": user_input}
    ]

    while True:

        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=messages,
            tools=tools,
            tool_choice="auto"
        )

        message = response.choices[0].message

        # Add assistant message
        messages.append(message)

        # Stop if no more tool calls
        if not message.tool_calls:
            break

        # Execute all tool calls
        for tool_call in message.tool_calls:

            function_name = tool_call.function.name
            function_args = json.loads(tool_call.function.arguments)

            function_to_call = available_functions[function_name]

            result = function_to_call(**function_args)

            # Add tool result
            messages.append({
                "role": "tool",
                "tool_call_id": tool_call.id,
                "content": str(result)
            })

    return messages[-1]["content"]
```

---

# 🔥 কী হচ্ছে এখানে?

## 🟢 while loop

এই loop AI-কে বারবার চিন্তা করতে দেয়:

```python id="3w0r8j"
while True:
```

---

## 🟡 LLM decides tools

LLM decide করে:

* কোন tool লাগবে
* কতগুলো tool লাগবে

---

## 🔵 Execute tools dynamically

```python id="k3ig32"
function_to_call = available_functions[function_name]
```

এখানে dynamically function execute হচ্ছে।

---

## 🟣 Tool results back to LLM

```python id="on7svw"
messages.append({
    "role": "tool",
    "tool_call_id": tool_call.id,
    "content": str(result)
})
```

👉 এটা খুব important
কারণ LLM tool output না পেলে next reasoning করতে পারবে না।

---

# 🎛️ 5. Gradio Interface

```python id="ptoz2l"
import gradio as gr

demo = gr.Interface(
    fn=run_agent,
    inputs="text",
    outputs="text",
    title="✈️ Multi-tool Airline AI Assistant"
)

demo.launch()
```

---

# 💬 Example Interaction

## User:

> Find cheapest flight from Dhaka to Dubai and book it for Sakib

---

## AI Workflow:

### Step 1:

Calls:

```python id="6c6u7n"
search_flights()
```

---

### Step 2:

Finds cheapest:

```python id="p0k4h6"
BG202 = 10000
```

---

### Step 3:

Calls:

```python id="uzm31p"
book_ticket(name="Sakib", flight_no="BG202")
```

---

### Final Response:

> “Your ticket has been booked successfully for BG202.”

---

# 🧠 Important Concept: Agentic Loop

এই pattern-টাই আসলে:

# 🔥 Agentic Workflow

কারণ AI:

* observes
* reasons
* acts
* observes again
* acts again

---

# 🚀 Real-world Examples

এই same architecture use হয়:

* AI customer support
* AI coding assistants
* autonomous research agents
* AI shopping agents
* AI travel assistants

---

# ⚡ One-line Summary

**Handling multiple tool calls means creating an agent loop where the LLM repeatedly reasons, calls tools, receives outputs, and continues until the task is complete.**
