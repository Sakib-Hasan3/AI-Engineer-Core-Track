# Inside LLaMA: PyTorch Model Architecture and Token Embeddings — বাংলায় (Deep Dive)

Llama 3.1 হলো একটি **Transformer-based decoder-only model**—যা PyTorch দিয়ে তৈরি এবং token embeddings থেকে শুরু করে attention পর্যন্ত সবকিছু ম্যাট্রিক্স অপারেশনের মাধ্যমে কাজ করে।

এই অংশে আমরা বুঝবো:

* Token Embedding কী
* LLaMA PyTorch architecture কেমন
* forward pass কীভাবে কাজ করে
* embeddings → attention → output flow

---

# ১. LLaMA Model কীভাবে শুরু হয়?

LLM-এর ইনপুট সবসময়:

```text id="l1"
Text → Token IDs → Embeddings
```

---

# ২. Token Embeddings কী?

Token embedding হলো:

> প্রতিটি token ID কে একটি dense vector-এ convert করা

---

## Example:

```text id="l2"
Token ID: 42
↓
Vector: [0.12, -0.98, 0.33, ...]
```

---

## Embedding Matrix:

E \in \mathbb{R}^{V \times d}

| Symbol | Meaning          |
| ------ | ---------------- |
| V      | vocabulary size  |
| d      | hidden dimension |

---

## Lookup operation:

x_i = E[\text{token}_i]

👉 simple indexing operation

---

# ৩. PyTorch Embedding Layer (Real Code)

```python id="p1"
import torch
import torch.nn as nn

vocab_size = 50000
hidden_size = 4096

embedding = nn.Embedding(vocab_size, hidden_size)

token_ids = torch.tensor([10, 20, 30])

embeddings = embedding(token_ids)

print(embeddings.shape)
```

---

## Output:

```text id="o1"
[3, 4096]
```

---

# ৪. LLaMA Architecture Overview

```text id="a1"
Token IDs
   ↓
Token Embedding
   ↓
Positional Encoding (RoPE)
   ↓
Transformer Blocks (×N)
   ↓
LayerNorm
   ↓
Linear Layer (LM Head)
   ↓
Logits
   ↓
Softmax
```

---

# ৫. Decoder-Only Transformer (LLaMA Style)

Llama 3.1 uses:

* **Decoder-only architecture**
* No encoder
* Autoregressive generation

---

# ৬. PyTorch LLaMA Block (Simplified)

```python id="p2"
class LLaMABlock(nn.Module):
    def __init__(self, hidden_size):
        super().__init__()
        self.attention = nn.MultiheadAttention(hidden_size, num_heads=32)
        self.ffn = nn.Sequential(
            nn.Linear(hidden_size, hidden_size * 4),
            nn.SiLU(),
            nn.Linear(hidden_size * 4, hidden_size)
        )
        self.ln1 = nn.LayerNorm(hidden_size)
        self.ln2 = nn.LayerNorm(hidden_size)

    def forward(self, x):
        attn_out, _ = self.attention(x, x, x)
        x = self.ln1(x + attn_out)

        ffn_out = self.ffn(x)
        x = self.ln2(x + ffn_out)

        return x
```

---

# ৭. Full LLaMA Model (Simplified)

```python id="p3"
class MiniLLaMA(nn.Module):
    def __init__(self, vocab_size, hidden_size, num_layers):
        super().__init__()

        self.embed = nn.Embedding(vocab_size, hidden_size)
        self.layers = nn.ModuleList([
            LLaMABlock(hidden_size) for _ in range(num_layers)
        ])
        self.ln_final = nn.LayerNorm(hidden_size)
        self.lm_head = nn.Linear(hidden_size, vocab_size)

    def forward(self, input_ids):
        x = self.embed(input_ids)

        for layer in self.layers:
            x = layer(x)

        x = self.ln_final(x)
        logits = self.lm_head(x)

        return logits
```

---

# ৮. Embedding → Context Understanding

Embedding শুধু number না—এটা semantic space:

```text id="e1"
king → [0.2, 0.8, -0.1]
queen → [0.21, 0.79, -0.12]
```

👉 meaning-based similarity

---

# ৯. Positional Encoding (RoPE in LLaMA)

LLaMA uses:

> Rotary Position Embedding (RoPE)

It injects position into attention.

LaMA

Self-attention decides:

> কোন word কোন word-এর সাথে কতটা related

“KV Cache inside LLaMA (step-by-step with math + PyTorch)”
👉 “How attention matrices are computed in real GPU execution”
👉 “Build a mini working LLaMA model from scratch”
